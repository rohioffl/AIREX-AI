version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - python3 -m pip install --upgrade pip
      - python3 -m pip install -e services/airex-core
  build:
    commands:
      - cd database
      - alembic upgrade head
