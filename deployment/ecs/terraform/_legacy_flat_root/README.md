# Legacy Flat Root

These files were the previous flat Terraform root.

They are archived for reference only.

Use this active production root instead:

- `deployment/ecs/terraform/environments/prod`
