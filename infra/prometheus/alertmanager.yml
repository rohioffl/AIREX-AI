route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 1h
  receiver: 'slack-notifications'

receivers:
  - name: 'slack-notifications'
    # Stub config for Slack webhook
    # slack_configs:
    #   - api_url: 'https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXXXXXX'
    #     channel: '#alerts'
    #     send_resolved: true

  - name: 'pagerduty-notifications'
    # Stub config for PagerDuty integration
    # pagerduty_configs:
    #   - service_key: 'YOUR_INTEGRATION_KEY'
    #     send_resolved: true
