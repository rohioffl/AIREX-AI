Task 6 - Update buildspec.frontend.yml for apps/web path

Changes made:
- npm ci --prefix frontend -> npm ci --prefix apps/web
- npm run build --prefix frontend -> npm run build --prefix apps/web
- aws s3 sync frontend/dist -> aws s3 sync apps/web/dist
- frontend/node_modules/**/* -> apps/web/node_modules/**/* (not found in this file)

Verification:
- File deployment/ecs/codebuild/buildspec.frontend.yml updated with new prefixes and paths
- All references replaced in the file, and zero occurrences of 'frontend/' found in the file

Final file content:
version: 0.2

env:
  variables:
    AWS_REGION: ap-south-1
    FRONTEND_BUCKET: airex-prod-frontend
    CLOUDFRONT_DISTRIBUTION_ID: REPLACE_ME

phases:
  install:
    runtime-versions:
      nodejs: 20
  build:
    commands:
      - npm ci --prefix apps/web
      - npm run build --prefix apps/web
  post_build:
    commands:
      - aws s3 sync apps/web/dist s3://$FRONTEND_BUCKET --delete
      - aws cloudfront create-invalidation --distribution-id $CLOUDFRONT_DISTRIBUTION_ID --paths "/*"
