# airex-core

Shared package for reusable AIREX domain models, schemas, service abstractions, and cross-cutting core modules.

Includes SaaS hierarchy models (**`Organization`**, **`Tenant`** with **`organization_id`**, memberships, projects) alongside RLS-scoped domain tables keyed by **`tenant_id`**.
